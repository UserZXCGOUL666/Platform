Исправления мобильного режима / предпросмотра смартфона:

1. Починен layout в режиме "Смартфон" для каталога:
   - toolbar перестроен;
   - select сортировки и кнопка фильтров больше не разъезжаются;
   - сетка карточек стабилизирована.

2. Починен экран чатов:
   - список диалогов теперь идёт отдельным верхним блоком;
   - окно переписки занимает всю ширину;
   - убрано сжатие контента в узкую колонку.

3. Починена админка в режиме смартфона:
   - вкладки сделаны горизонтально прокручиваемыми;
   - карточки метрик и блоки аналитики адаптированы;
   - строки объявлений не ломают сетку.

4. Починены общие отступы и ширины в смартфон-режиме.
